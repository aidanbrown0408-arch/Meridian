# Phase 1 dependencies
pandas>=2.0
numpy>=1.24
PyYAML>=6.0
yfinance>=0.2.40      # stock OHLCV
ccxt>=4.2             # crypto OHLCV (Binance public API)

# Phase 3 dependencies
Jinja2>=3.1           # daily HTML dashboard template

# Phase 5: agents/sentiment_agent.py talks to Alpha Vantage over stdlib
# urllib (same approach as utils/slack.py) -- no new dependency needed.
